[
    {"id": "q1", "query": "SELECT * FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE message LIKE '%queen%' ORDER BY timestamp DESC limit 100;"},
    {"id": "q2", "query": "SELECT * FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE cloud.region = 'us-east-2' ORDER BY timestamp DESC limit 100;"},
    {"id": "q3", "query": "SELECT * FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE message LIKE '%queen%' and cloud.region = 'us-east-2'  ORDER BY timestamp DESC limit 100;"},
    {"id": "q4", "query": "SELECT window(`timestamp`, '2 hours').start as time_window, level, COUNT(*) as cnt FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` GROUP BY window(`timestamp`, '2 hours').start, level;"},
    {"id": "q5", "query": "SELECT window(`timestamp`, '2 hours').start as time_window, level, COUNT(*) as cnt FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE message LIKE '%queen%' GROUP BY window(`timestamp`, '2 hours').start, level;"},
    {"id": "q6", "query": "SELECT window(`timestamp`, '2 hours').start as time_window, level, COUNT(*) as cnt FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE cloud.region = 'us-east-2' GROUP BY window(`timestamp`, '2 hours').start, level;"},
    {"id": "q7", "query": "SELECT window(`timestamp`, '2 hours').start as time_window, level, COUNT(*) as cnt FROM `local`.`default`.`quickwit_generated_logs_v1_iceberg_005` WHERE message LIKE '%queen%' and cloud.region = 'us-east-2' GROUP BY window(`timestamp`, '2 hours').start, level;"}
]